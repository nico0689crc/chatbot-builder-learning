[phases.install]
cmds = ["cd fase-1/ejercicios && npm install"]

[phases.build]
cmds = ["cd fase-1/ejercicios && npx prisma generate && npm run build"]

[start]
cmd = "cd fase-1/ejercicios && node -r tsconfig-paths/register dist/fase-1/ejercicios/src/index.js"
