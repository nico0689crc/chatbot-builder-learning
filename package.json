{
  "name": "chatbot-builder",
  "scripts": {
    "build": "cd fase-2/ejercicios && npm install && npx prisma generate && npx prisma migrate deploy && npm run build",
    "start": "cd fase-2/ejercicios && ts-node -r tsconfig-paths/register --transpile-only src/index.ts"
  }
}