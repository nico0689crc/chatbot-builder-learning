{
  "name": "chatbot-builder",
  "scripts": {
    "build": "cd fase-1/ejercicios && npm install && npx prisma generate && npm run build",
    "start": "cd fase-1/ejercicios && node -r tsconfig-paths/register dist/fase-1/ejercicios/src/index.js"
  }
}
